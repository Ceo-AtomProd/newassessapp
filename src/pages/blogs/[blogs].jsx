import blogData from "@/libs/blogData";

function Blogs() {
    const blogs = blogData
    return (
        // {blogs.map(i=><div>
        //     Enter
        // </div>
        // )}
        <div></div>
    );
}


export default Blogs;